import EventsHeader from './EventsHeader';
import './Events.css';

const Events = () => {
    return (
        <div className="events-page">
            <EventsHeader />
        </div>
    );
};

export default Events;
